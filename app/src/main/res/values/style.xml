<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Colores personalizados para DatePicker -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <!-- Estilos personalizados para DatePicker -->
        <item name="android:datePickerDialogTheme">@style/AppTheme.DatePickerDialog</item>
        <item name="android:dialogTheme">@style/AppTheme.Dialog</item>
    </style>

    <!-- Estilo personalizado para el DatePickerDialog -->
    <style name="AppTheme.DatePickerDialog" parent="ThemeOverlay.MaterialComponents.Dialog">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorOnPrimary">@color/white</item>
    </style>

    <!-- Estilo personalizado para los diálogos -->
    <style name="AppTheme.Dialog" parent="ThemeOverlay.MaterialComponents.Dialog">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorOnPrimary">@color/white</item>
    </style>

    <style name="Title">
        <item name="android:textSize">18sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="Text">
        <item name="android:textSize">14sp</item>
    </style>

    <style name="ButtonTextStyle">
        <item name="android:textSize">16sp</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:fontFamily">@font/inter_semi_bold</item>
    </style>

    <style name="Outlined">
        <item name="android:textColor">@color/colorPrimary</item>
        <item name="android:textAppearance">@style/ButtonTextStyle</item>
        <item name="android:paddingTop">8dp</item>
        <item name="android:paddingBottom">8dp</item>
        <item name="android:layout_margin">16dp</item>
        <item name="android:background">@drawable/bg_button_outlined</item>
    </style>

    <style name="Button">
        <item name="android:textColor">@color/white</item>
        <item name="android:textAppearance">@style/ButtonTextStyle</item>
        <item name="android:paddingTop">8dp</item>
        <item name="android:paddingBottom">8dp</item>
        <item name="android:layout_margin">16dp</item>
        <item name="android:background">@drawable/bg_button</item>
    </style>

</resources>